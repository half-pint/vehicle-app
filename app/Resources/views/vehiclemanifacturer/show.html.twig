{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vehiclemanifacturer</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicleManifacturer.id }}</td>
            </tr>
            <tr>
                <th>Manifacturer</th>
                <td>{{ vehicleManifacturer.manifacturer }}</td>
            </tr>
            <tr>
                <th>Vehiclemodel</th>
                <td>{{ vehicleManifacturer.vehicleModel }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vehiclemanifacturer_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vehiclemanifacturer_edit', { 'id': vehicleManifacturer.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
